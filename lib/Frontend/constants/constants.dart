// Colors
import 'package:flutter/material.dart';

const Color kPrimaryColor =
    Color(0xff151154); // This is the main Color of the App

const Color kPrimaryLightColor =
    Color(0xff262561); // This is the main Color of the App

const Color kPrimaryButtonColor =
    Color(0xffFF677F); // This is the main Color of the add button

const Color kTextFieldColor = Color(0xff262561);
